import { Injectable } from '@angular/core';
//import { Apollo } from 'apollo-angular';
//import { takeUntil, filter, share, map } from 'rxjs/operators';

//import { TOP_MENU_ITEMS_QUERY } from '../../apollo/top-menu/top-menu-items';
//import { TopMenuResponse, MenuItem, BannerItem, Language, Footer } from '../../apollo/models/base-models';
//import { Observable } from 'apollo-link';

@Injectable({
  providedIn: 'root'
})
export class InitialDataService {

  constructor(
    //private apollo: Apollo
  ) { }

  fetchData() {
    
  }
}